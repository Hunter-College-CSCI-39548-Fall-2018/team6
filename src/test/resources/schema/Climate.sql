CREATE TABLE IF NOT EXISTS Climate (
  city_name Varchar(32),
  state_name Varchar(32),
  avg_q1 float,
  avg_q2 float,
  avg_q3 float,
  avg_q4 float,
  avg_pnnual_precipitation int,
  avg_precipitation_days int,
  avg_snowfall float,
  avg_days_snowfall int
);